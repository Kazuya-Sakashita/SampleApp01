<p id="notice"><%= notice %></p>

<h1 class="text-white text-5xl text-center bg-indigo-500 rounded ">読上げ掲示板</h1>
<p>
<hr>
使用方法の説明
<hr>
New Post ー＞　Create Postでコンテントを追加できます。<br>
登録後、再生ボタンで読み上げができます。停止ボタンで読み上げ停止ができます。<br><br>

すでに登録されているコンテントの再生は、Show　ー＞再生ボタンで読み上げが可能です。<br><br><br><br>
</p>
<hr>
<%= search_form_for @q do |f| %>
  <%= f.label :"category" %>
  <%= f.search_field :category_cont %>

  <%= f.submit %>
<% end %>

<hr>
<table>
  <thead>
  <tr>
    <th>Category</th>
    <th>Content</th>
    <th>Author</th>
    <th colspan="3"></th>
  </tr>
  </thead>

  <tbody>
  <% @posts.each do |post| %>
    <tr>
      <td><%= post.category %></td>
      <td>
        <%= link_to post do %><%= post.content %>
        <% end %>
      </td>
      <td><%= post.author %></td>
      <td><%= link_to 'Show', post %></td>
      <td><%= link_to 'Edit', edit_post_path(post) %></td>
      <!--        <td><%#= link_to 'Destroy', post, method: :delete, data: { confirm: 'Are you sure?' } %></td>-->
    </tr>
  <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Post', new_post_path %>



<% @posts.each do |post| %>
  <section class="text-gray-600 body-font">
    <div class="container px-5 py-5 mx-auto">
      <div class="flex flex-wrap -mx-4 -my-8">
        <div class="py-8 px-4 lg:w-1/3">
          <div class="h-full flex items-start">
            <div class="w-12 flex-shrink-0 flex flex-col text-center leading-none">
              <span class="text-gray-500 pb-2 mb-2 border-b-2 border-gray-200"><%= post.category %></span>
              <%# <span class="font-medium text-lg text-gray-800 title-font leading-none"></span> %>
            </div>

            <div class="flex-grow pl-6">
              <h2 class="tracking-widest text-xs title-font font-medium text-indigo-500 mb-1">CONTENT</h2>
              <%= link_to post do %>
                <h1 class="title-font text-xl font-medium text-gray-900 mb-3 bg-indigo-100 border-0 py-2 px-8 rounded hover:bg-gray-100"><%= post.content %></h1>
              <% end %>
              <div>
                <div class="flex space-x-2">
                  <%= link_to post,class:"title-font text-xl font-medium text-white mb-3 bg-indigo-500 border-0 py-2 px-8 focus:outline-none hover:bg-indigo-600 rounded text-lg" do %>
                    <div class="text-white"><span>SHOW</span></div>
                  <% end %>
                  <%= link_to edit_post_path(post),class:"title-font text-xl font-medium text-white mb-3 bg-indigo-500 border-0 py-2 px-8 focus:outline-none hover:bg-indigo-600 rounded text-lg" do %>
                    <div class="text-white"><span>Edit</span></div>
                  <% end %>
                </div>
                <div>
                  <div>
                  </div>
                  <div>
                    <p class="leading-relaxed mb-5"></p>
                    <span class="flex-grow flex flex-col pl-3">
                <span class="title-font font-medium text-gray-900">投稿者:<%= post.author %></span>
              </span>
                  </div>
                </div>
              </div>
            </div>
          </div>
        </div>
  </section>
<% end %>