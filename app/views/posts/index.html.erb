<div class="flex flex-col">
  <div>
    <p id="notice"><%= notice %></p>
  </div>
  <div>
    <div class="text-gray-600 body-font">
      <div class="container px-5 py-24 mx-auto">
        <div class="flex flex-col text-center w-full mb-20">
          <h1 class="sm:text-4xl text-3xl font-medium title-font mb-2 text-gray-900">読上げ掲示板</h1>
          <p class="lg:w-2/3 mx-auto leading-relaxed text-base">入力した文面を保存し、音声で読み上げてくれます。保存した文面は、何度でも呼び出すことができます。</p>
        </div>
        <div class="border-2 border-gray-600 m-10">
          <div class="max-w-sm w-full lg:max-w-full lg:flex">
            <div class="font-bold text-xl mb-2">使用方法の説明</div>
            <p class="lg:w-2/3 mx-auto leading-relaxed text-base">
              1.New Post ー＞　Create Postでコンテントを追加できます。<br>
              2.登録後、再生ボタンで読み上げができます。停止ボタンで読み上げ停止ができます。<br><br>
              3.すでに登録されているコンテントの再生は、Show　ー＞再生ボタンで読み上げが可能です。
            </p>
          </div>
        </div>
      </div>

      <div class="container px-5 py-5 mx-auto">
        <div class="p-2 m-2">
          <%= link_to '新しく言葉を登録する', new_post_path, class: "inline-flex text-white bg-teal-500 border-0 py-2 px-6 focus:outline-none hover:bg-teal-600 rounded text-lg" %>
        </div>
        <table class="table-auto w-full text-left whitespace-no-wrap  ">
          <thead>
          <tr>
            <th class="px-4 py-3 title-font tracking-wider font-medium text-gray-900 text-sm bg-gray-100 rounded-tl rounded-bl">カテゴリ</th>
            <th class="px-4 py-3 title-font tracking-wider font-medium text-gray-900 text-sm bg-gray-100">内容</th>
            <th class="px-4 py-3 title-font tracking-wider font-medium text-gray-900 text-sm bg-gray-100">投稿者</th>
            <th class="w-10 title-font tracking-wider font-medium text-gray-900 text-sm bg-gray-100 rounded-tr rounded-br">編集</th>
          </tr>
          </thead>

          <tbody class="bg-white divide-y divide-gray-200">
          <% @posts.each do |post| %>
            <tr class="flex flex-col flex-no wrap sm:table-row mb-2 sm:mb-0">
              <td class="border-t-2 border-gray-200 px-4 py-3"><%= post.title %></td>
              <td class="border-t-2 border-gray-200 px-4 py-3"><%= post.content %></td>
              <td class="border-t-2 border-gray-200 px-4 py-3"><%= post.author %></td>
              <td class="px-4 py-3 text-lg text-gray-900">
                <%= link_to 'Show', post, class: "inline-flex text-white bg-teal-500 border-0 py-2 px-6 focus:outline-none hover:bg-teal-600 rounded text-lg" %>
                <%= link_to 'Edit', edit_post_path(post), class: "inline-flex text-white bg-teal-500 border-0 py-2 px-6 focus:outline-none hover:bg-teal-600 rounded text-lg" %>
              </td>
              <!--        <td><%#= link_to 'Destroy', post, method: :delete, data: { confirm: 'Are you sure?' } %></td>-->
            </tr>
          <% end %>
          </tbody>
        </table>

        <br>
      </div>
      <div>
      </div>
    </div>
  </div>


